You need to do following fixes:

1. In add property view, enter property details manually should be selected.
2. In Properties & Units screen if no properties added screen is being displayed then do not display the top buttons of import csv and add property
3. In terms and privacy screen of onboarding for some reason their is a scrollbar coming at right even though there are spaces in top and bottom
4. In onboarding first step, "Tell us about your company" add relevant placeholders to all fields
5. in verify your email/otp verification screens the otp inputs are appeared in left most not at the center of its parent container

Before solving each problem analyze properly what user wants to achieve and do accordingly, do not add code snippets which should be considered as bad practise or slop.
