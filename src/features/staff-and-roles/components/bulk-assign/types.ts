export type StaffMember = {
  id: number;
  name: string;
  role: string;
  selected: boolean;
};

export type Property = {
  id: number;
  name: string;
  address: string;
  units: string;
  selected: boolean;
};
